<div id="favorite_form">
    <% if logged_in? %>
        <% if current_user.favorited?(current_cultivar) %>
            <%= render 'map/unfavorite', current_cultivar: current_cultivar %>
        <% else %>
            <%= render 'map/add_favorite', current_cultivar: current_cultivar %>
        <% end %>
    <% else %>
        <%= link_to "log in", login_path %>
    <% end %>
</div>
