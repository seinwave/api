!!!
%html
  %head
    %title Roses @ BBG
    :javascript
      const appConfig = {
        mapboxToken: "#{Rails.application.credentials.mapbox[:access_token]}",
      };
      const roseIconPath = "#{asset_path('rose-icon.svg')}";
    = javascript_include_tag 'map'
    = stylesheet_link_tag "map", "data-turbo-track" => "reload"

    

  %body
    = render(SearchBarComponent.new(query: ""))
    %div{ id: "map", style: "width: 100%; height: 100%; position: relative;" }
